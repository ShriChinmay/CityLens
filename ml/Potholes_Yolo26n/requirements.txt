# ML module dependencies (only what's needed to run inference)
# .pt (backend, PyTorch): needs ultralytics + opencv
ultralytics>=8.4,<9
opencv-python>=4.8
# .onnx (edge / lightweight): add onnxruntime to run the ONNX model
onnxruntime>=1.17
